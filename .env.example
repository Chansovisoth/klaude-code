# klaude-code environment secrets.
# scripts/install.sh copies this to .env and fills in generated values.
# .env is gitignored — never commit it.

# SearXNG cookie/session secret (any long random hex string)
SEARXNG_SECRET=

# room to grow: future service credentials go here, e.g.
# CRAWL4AI_API_TOKEN=
