[project]
name = "klaude-web"
version = "0.2.0a3"
description = "klaude-code web layer: quality-routed search + fetch cascade"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "trafilatura>=1.9", "ddgs>=9", "mcp>=1.0", "klaude-core"]

[project.scripts]
klaude-web-mcp = "klaude_web.mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/klaude_web"]
